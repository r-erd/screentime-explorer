<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Do NOT enable com.apple.security.app-sandbox here.
    Sandboxed apps cannot access knowledgeC.db regardless of FDA grant.
    This app is distributed outside the Mac App Store and does not use the sandbox.
  -->

  <!-- Hardened runtime: allow JIT for V8 / Chromium -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- Allow unsigned executable memory (required by Electron/V8) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Allow DYLD environment variables (required by Electron) -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
